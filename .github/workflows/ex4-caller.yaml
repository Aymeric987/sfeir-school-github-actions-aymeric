name: run npm test with reusable workflow
on:
  workflow_dispatch:

env:
  NODE_VERSION: 20

jobs:
  call-reusable-workflow:
    uses: Aymeric987/sfeir-school-github-actions-aymeric/.github/workflows/ex4.yaml@main
    with:
      node-version: ${env.NODE_VERSION}